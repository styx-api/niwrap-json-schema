{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A script to segment subjects (notal)",
  "type": "object",
  "properties": {
    "__STYXTYPE__": {
      "const": "segment_subject_notal"
    },
    "subject_path": {
      "type": "string",
      "title": "Subject Path",
      "description": "Path to the subject's directory"
    }
  },
  "additionalProperties": false,
  "required": [
    "subject_path"
  ]
}