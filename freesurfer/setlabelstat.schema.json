{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "setlabelstat",
  "description": "Replaces the stat values in a label file with the single stat value supplied on the command-line.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "freesurfer/setlabelstat"
    },
    "inlabelfile": {
      "type": "string",
      "x-styx-type": "path",
      "description": "Input label file.",
      "title": "Input Label File"
    },
    "outlabelfile": {
      "type": "string",
      "x-styx-type": "path",
      "description": "Output label file.",
      "title": "Output Label File"
    },
    "statval": {
      "type": "number",
      "description": "Stat value to replace in the label file.",
      "title": "Stat Value"
    },
    "help": {
      "type": "boolean",
      "description": "Displays help information.",
      "title": "Help Flag",
      "default": false
    }
  },
  "required": [
    "@type",
    "inlabelfile",
    "outlabelfile",
    "statval"
  ]
}