{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A command-line tool that processes configuration files and arguments.",
  "type": "object",
  "properties": {
    "__STYXTYPE__": {
      "const": "rca-config"
    },
    "source_config": {
      "type": "string",
      "title": "Source configuration file",
      "description": "Path to the source configuration file"
    },
    "updated_config": {
      "type": "string",
      "title": "Updated configuration file",
      "description": "Path to the updated configuration file"
    },
    "unknown_args_file": {
      "type": "string",
      "title": "Unknown arguments file",
      "description": "Path to the file where unknown arguments are recorded"
    },
    "args": {
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Arguments",
      "description": "Additional arguments to be processed"
    }
  },
  "additionalProperties": false,
  "required": [
    "source_config",
    "updated_config",
    "unknown_args_file"
  ]
}