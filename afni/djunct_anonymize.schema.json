{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "@djunct_anonymize",
  "description": "Helper program to anonymize files",
  "type": "object",
  "properties": {
    "@type": {
      "const": "afni/@djunct_anonymize"
    },
    "input": {
      "type": "string",
      "x-styx-type": "path",
      "description": "Input dataset",
      "title": "Input file"
    },
    "add_note": {
      "type": "string",
      "description": "Add a note to the history after anonymizing",
      "title": "Add Note"
    },
    "copy_to": {
      "type": "string",
      "x-styx-type": "path",
      "description": "Copy the input to a new file, which is then anonymized",
      "title": "Copy To"
    },
    "overwrite": {
      "type": "boolean",
      "description": "Overwrite the existing file if using -copy_to",
      "title": "Overwrite",
      "default": false
    }
  },
  "required": [
    "@type",
    "input"
  ]
}