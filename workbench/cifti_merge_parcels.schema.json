{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MERGE CIFTI FILES ALONG PARCELS DIMENSION",
  "description": "The input cifti files must have matching mappings along the direction not specified, and the mapping along the specified direction must be parcels.  The direction can be either an integer starting from 1, or the strings 'ROW' or 'COLUMN'.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/cifti_merge_parcels"
    },
    "direction": {
      "type": "string",
      "description": "which dimension to merge along (integer, 'ROW', or 'COLUMN')"
    },
    "cifti_out": {
      "type": "string",
      "description": "the output cifti file"
    },
    "cifti": {
      "type": "array",
      "items": {
        "type": "string",
        "x-styx-type": "path"
      },
      "description": "specify an input cifti file"
    }
  },
  "required": [
    "@type",
    "direction",
    "cifti_out",
    "cifti"
  ]
}