{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SURFACE GENERATE INFLATED",
  "description": "Generate inflated and very inflated surfaces. The output surfaces are 'matched' (have same XYZ range) to the anatomical surface. In most cases, an iterations-scale of 1.0 (default) is sufficient.  However, if the surface contains a large number of vertices (150,000), try an iterations-scale of 2.5.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/surface_generate_inflated"
    },
    "anatomical_surface_in": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the anatomical surface"
    },
    "inflated_surface_out": {
      "type": "string",
      "description": "the output inflated surface"
    },
    "very_inflated_surface_out": {
      "type": "string",
      "description": "the output very inflated surface"
    },
    "iterations_scale": {
      "type": "number",
      "description": "optional iterations scaling"
    }
  },
  "required": [
    "@type",
    "anatomical_surface_in",
    "inflated_surface_out",
    "very_inflated_surface_out"
  ]
}