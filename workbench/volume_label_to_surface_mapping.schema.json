{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MAP A LABEL VOLUME TO A SURFACE LABEL FILE",
  "description": "Map label volume data to a surface.  If -ribbon-constrained is not specified, uses the enclosing voxel method.  The ribbon mapping method constructs a polyhedron from the vertex's neighbors on each surface, and estimates the amount of this polyhedron's volume that falls inside any nearby voxels, to use as the weights for a popularity comparison.  If -thin-columns is specified, the polyhedron uses the edge midpoints and triangle centroids, so that neighboring vertices do not have overlapping polyhedra.  This may require increasing -voxel-subdiv to get enough samples in each voxel to reliably land inside these smaller polyhedra.  The volume ROI is useful to exclude partial volume effects of voxels the surfaces pass through, and will cause the mapping to ignore voxels that don't have a positive value in the mask.  The subdivision number specifies how it approximates the amount of the volume the polyhedron intersects, by splitting each voxel into NxNxN pieces, and checking whether the center of each piece is inside the polyhedron.  If you have very large voxels, consider increasing this if you get unexpected unlabeled vertices in your output.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/volume_label_to_surface_mapping"
    },
    "volume": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the volume to map data from"
    },
    "surface": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the surface to map the data onto"
    },
    "label_out": {
      "type": "string",
      "description": "the output gifti label file"
    },
    "ribbon_constrained": {
      "type": "object",
      "properties": {
        "inner_surf": {
          "type": "string",
          "x-styx-type": "path",
          "description": "the inner surface of the ribbon"
        },
        "outer_surf": {
          "type": "string",
          "x-styx-type": "path",
          "description": "the outer surface of the ribbon"
        },
        "volume_roi": {
          "type": "string",
          "x-styx-type": "path",
          "description": "use a volume roi"
        },
        "dilate_missing": {
          "type": "number",
          "description": "use dilation for small vertices that 'missed' the geometry tests"
        },
        "voxel_subdiv": {
          "type": "integer",
          "description": "voxel divisions while estimating voxel weights"
        },
        "thin_columns": {
          "type": "boolean",
          "description": "use non-overlapping polyhedra",
          "default": false
        }
      },
      "required": [
        "inner_surf",
        "outer_surf"
      ],
      "description": "use ribbon constrained mapping algorithm"
    },
    "subvol_select": {
      "type": "string",
      "description": "select a single subvolume to map"
    }
  },
  "required": [
    "@type",
    "volume",
    "surface",
    "label_out"
  ]
}