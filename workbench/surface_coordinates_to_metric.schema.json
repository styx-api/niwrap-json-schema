{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MAKE METRIC FILE OF SURFACE COORDINATES",
  "description": "Puts the coordinates of the surface into a 3-map metric file, as x, y, z.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/surface_coordinates_to_metric"
    },
    "surface": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the surface to use the coordinates of"
    },
    "metric_out": {
      "type": "string",
      "description": "the output metric"
    }
  },
  "required": [
    "@type",
    "surface",
    "metric_out"
  ]
}