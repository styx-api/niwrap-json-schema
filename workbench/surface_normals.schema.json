{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "OUTPUT VERTEX NORMALS AS METRIC FILE",
  "description": "Computes the normal vectors of the surface file, and outputs them as a 3 column metric file.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/surface_normals"
    },
    "surface": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the surface to output the normals of"
    },
    "metric_out": {
      "type": "string",
      "description": "the normal vectors"
    }
  },
  "required": [
    "@type",
    "surface",
    "metric_out"
  ]
}