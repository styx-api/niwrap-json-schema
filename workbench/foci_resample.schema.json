{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PROJECT FOCI TO A DIFFERENT SURFACE",
  "description": "Unprojects foci from the <current-surf> for the structure, then projects them to <new-surf>.  If the foci have meaningful distances above or below the surface, use anatomical surfaces.  If the foci should be on the surface, use registered spheres and the options -discard-distance-from-surface and -restore-xyz.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/foci_resample"
    },
    "foci_in": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the input foci file"
    },
    "foci_out": {
      "type": "string",
      "description": "the output foci file"
    },
    "left_surfaces": {
      "type": "object",
      "properties": {
        "current_surf": {
          "type": "string",
          "x-styx-type": "path",
          "description": "the surface the foci are currently projected on"
        },
        "new_surf": {
          "type": "string",
          "x-styx-type": "path",
          "description": "the surface to project the foci onto"
        }
      },
      "required": [
        "current_surf",
        "new_surf"
      ],
      "description": "the left surfaces for resampling"
    },
    "right_surfaces": {
      "type": "object",
      "properties": {
        "current_surf": {
          "type": "string",
          "x-styx-type": "path",
          "description": "the surface the foci are currently projected on"
        },
        "new_surf": {
          "type": "string",
          "x-styx-type": "path",
          "description": "the surface to project the foci onto"
        }
      },
      "required": [
        "current_surf",
        "new_surf"
      ],
      "description": "the right surfaces for resampling"
    },
    "cerebellum_surfaces": {
      "type": "object",
      "properties": {
        "current_surf": {
          "type": "string",
          "x-styx-type": "path",
          "description": "the surface the foci are currently projected on"
        },
        "new_surf": {
          "type": "string",
          "x-styx-type": "path",
          "description": "the surface to project the foci onto"
        }
      },
      "required": [
        "current_surf",
        "new_surf"
      ],
      "description": "the cerebellum surfaces for resampling"
    },
    "discard_distance_from_surface": {
      "type": "boolean",
      "description": "ignore the distance the foci are above or below the current surface",
      "default": false
    },
    "restore_xyz": {
      "type": "boolean",
      "description": "put the original xyz coordinates into the foci, rather than the coordinates obtained from unprojection",
      "default": false
    }
  },
  "required": [
    "@type",
    "foci_in",
    "foci_out"
  ]
}