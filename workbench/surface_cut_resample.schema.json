{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RESAMPLE A CUT SURFACE",
  "description": "Resamples a surface file, given two spherical surfaces that are in register.  Barycentric resampling is used, because it is usually better for resampling surfaces, and because it is needed to figure out the new topology anyway.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/surface_cut_resample"
    },
    "surface_in": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the surface file to resample"
    },
    "current_sphere": {
      "type": "string",
      "x-styx-type": "path",
      "description": "a sphere surface with the mesh that the input surface is currently on"
    },
    "new_sphere": {
      "type": "string",
      "x-styx-type": "path",
      "description": "a sphere surface that is in register with <current-sphere> and has the desired output mesh"
    },
    "surface_out": {
      "type": "string",
      "description": "the output surface file"
    }
  },
  "required": [
    "@type",
    "surface_in",
    "current_sphere",
    "new_sphere",
    "surface_out"
  ]
}