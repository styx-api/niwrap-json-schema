{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MERGE METRIC FILES INTO A NEW FILE",
  "description": "Takes one or more metric files and constructs a new metric file by concatenating columns from them.  The input metric files must have the same number of vertices and same structure.\n\nExample: wb_command -metric-merge out.func.gii -metric first.func.gii -column 1 -metric second.func.gii\n\nThis example would take the first column from first.func.gii, followed by all columns from second.func.gii, and write these columns to out.func.gii.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/metric_merge"
    },
    "metric_out": {
      "type": "string",
      "description": "the output metric"
    },
    "metric": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "metric_in": {
            "type": "string",
            "x-styx-type": "path",
            "description": "a metric file to use columns from"
          },
          "column": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "column": {
                  "type": "string",
                  "description": "the column number or name"
                },
                "up_to": {
                  "type": "object",
                  "properties": {
                    "last_column": {
                      "type": "string",
                      "description": "the number or name of the last column to include"
                    },
                    "reverse": {
                      "type": "boolean",
                      "description": "use the range in reverse order",
                      "default": false
                    }
                  },
                  "required": [
                    "last_column"
                  ],
                  "description": "use an inclusive range of columns"
                }
              },
              "required": [
                "column"
              ]
            },
            "description": "select a single column to use"
          }
        },
        "required": [
          "metric_in",
          "column"
        ]
      },
      "description": "specify an input metric"
    }
  },
  "required": [
    "@type",
    "metric_out",
    "metric"
  ]
}