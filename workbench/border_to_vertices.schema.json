{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DRAW BORDERS AS VERTICES IN A METRIC FILE",
  "description": "Outputs a metric with 1s on vertices that follow a border, and 0s elsewhere.  By default, a separate metric column is created for each border.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/border_to_vertices"
    },
    "surface": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the surface the borders are drawn on"
    },
    "border_file": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the border file"
    },
    "metric_out": {
      "type": "string",
      "description": "the output metric file"
    },
    "border": {
      "type": "string",
      "description": "create ROI for only one border"
    }
  },
  "required": [
    "@type",
    "surface",
    "border_file",
    "metric_out"
  ]
}