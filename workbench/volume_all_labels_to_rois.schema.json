{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MAKE ROIS FROM ALL LABELS IN A VOLUME FRAME",
  "description": "The output volume has a frame for each label in the specified input frame, other than the ??? label, each of which contains an ROI of all voxels that are set to the corresponding label.",
  "type": "object",
  "properties": {
    "@type": {
      "const": "workbench/volume_all_labels_to_rois"
    },
    "label_in": {
      "type": "string",
      "x-styx-type": "path",
      "description": "the input volume label file"
    },
    "map": {
      "type": "string",
      "description": "the number or name of the label map to use"
    },
    "volume_out": {
      "type": "string",
      "description": "the output volume file"
    }
  },
  "required": [
    "@type",
    "label_in",
    "map",
    "volume_out"
  ]
}